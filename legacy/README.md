# Legacy config

Retired configuration, kept for reference — **not linked by `install.sh`, not active.**

- **`.vimrc`** / **`.vim/`** — the previous classic-Vim setup (vimscript, `vim-plug`).
  Replaced by the Neovim config under `dots/.config/nvim/`.

Preserved deliberately so it stays browsable. Kept here rather than relying on git
history alone. See `docs/adr/0001-neovim-over-vim.md` for why it was retired.
